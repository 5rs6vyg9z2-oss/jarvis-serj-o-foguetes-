services:
  postgres:
    image: postgres:16
    container_name: jarvis_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - jarvis_postgres_data:/var/lib/postgresql/data

volumes:
  jarvis_postgres_data:
    name: jarvis_postgres_data_v3
